

const LectureManage = () => {
    const token = localStorage.getItem('token')
	if(!token){
    window.location.replace("/");
	}else{
  return (
    <div>
      
    </div>
  )
}}

export default LectureManage
